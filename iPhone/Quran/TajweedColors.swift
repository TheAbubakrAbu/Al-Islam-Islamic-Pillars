import SwiftUI

struct TajweedColors {
    // Silent / merged / omitted family
    static let laamShamsiyyah    = Color(red: 0.42, green: 0.47, blue: 0.56) // muted slate
    static let hamzatulWasl      = Color(red: 0.56, green: 0.61, blue: 0.69) // lighter slate
    static let silentLetter      = Color(red: 0.66, green: 0.69, blue: 0.74) // soft gray
    static let idghamBilaGhunnah = Color(red: 0.34, green: 0.39, blue: 0.47) // darker slate

    // Heavy / qalqalah family
    static let heavy             = Color(red: 0.23, green: 0.29, blue: 0.70) // deep indigo-blue
    static let qalqalah          = Color(red: 0.16, green: 0.45, blue: 0.89) // brighter blue

    // Ghunnah family
    static let idghamBiGhunnahLight = Color(red: 0.39, green: 0.73, blue: 0.46) // light green
    static let idghamBiGhunnahHeavy = Color(red: 0.22, green: 0.65, blue: 0.35) // medium green
    static let ikhfaa               = Color(red: 0.13, green: 0.67, blue: 0.56) // teal-green
    static let iqlab                = Color(red: 0.10, green: 0.55, blue: 0.34) // darker emerald

    // Madd family
    static let maddNatural2      = Color(red: 0.95, green: 0.69, blue: 0.20) // amber
    static let maddEnding246     = Color(red: 0.94, green: 0.55, blue: 0.18) // orange
    static let maddConnected     = Color(red: 0.92, green: 0.40, blue: 0.23) // coral-orange
    static let maddDisconnected  = Color(red: 0.88, green: 0.31, blue: 0.24) // red-orange
    static let maddLazim         = Color(red: 0.76, green: 0.20, blue: 0.21) // deep red
}
